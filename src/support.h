/*
 * defines for the support functions.
 */

void backslash_n_to_linefeed(char *s0, char *s, int max_len);
void trim_string(char *s0, char *s, int max_len);
void Xdialog_array(gint elements);
int my_scanf(char *buf);

